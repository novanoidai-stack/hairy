// Stub nativo: la reserva de grupo vive solo en la web por ahora (portal público
// es solo web). Mismo criterio que otros componentes del portal.
export function PortalGrupoModal(): null { return null; }
