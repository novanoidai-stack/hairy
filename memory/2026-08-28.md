# 2026-08-28 — Re-análisis post-refactors, Fase 5 y CI verde

## Contexto
Otra sesión ejecutó en paralelo: TanStack Query + lib/datos/ (43f757fc0), regla de
ocupación única (eb7e7404c), partir el monolito 25.7K→17.8K (c359c7a9b), seguridad+CI
con E2E (648f99b07), NewCitaModal.web.tsx (4.6K líneas). Trabajando en el MISMO árbol
— hubo colisiones: commiteé su NewCitaModal sin los imports y rompí CI dos veces.

## Lección clave (2ª vez)
**El typecheck local NO es evidencia.** El working tree tiene .expo generados y
cambios de sesiones paralelas sin commitear. Antes de pushear: `git archive HEAD |
tar -x` a /tmp + copiar node_modules + tsc. Eso replica la CI exactamente.

## Hecho hoy
- Re-análisis completo (Explore): nada roto, store sin mezcla, sin duplicados de la regla.
- Riesgo documentado: queryClient.ts decía "realtime → setQueryData" pero ese puente
  NO existe. Corregido el comentario.
- Higiene: ~45MB de raíz → archive/raiz-legacy/ (PDFs ajenos, temp_*, 8 PNGs,
  scratch*, design-demos, html_pages, project). llms*.txt se quedan (los usa
  test-e2e-seo). tsconfig excluye archive/.
- xlsx: vendor/xlsx-0.20.3.tgz con file: — build ya no depende de cdn.sheetjs.com.
- Fase 5 (mi parte): WeekView+MonthView+ClienteHistorialModal →
  components/agenda/views/VistasSemanaMes.web.tsx (1.410 líneas); Icon → ui/Icon.web.tsx;
  BLOQUEO_COLORS/LABELS → lib/agendaBloqueUi.ts. AgendaCalendar: 17.841 → 11.827.
- CI verde en 83d43fc64 (incluye E2E agenda-demo con el selector visible arreglado).

## Pendiente
- USUARIO (crítico): rotar service_role key de Supabase (viva en historial git) y
  credenciales E2E/playwright; rotar token Vercel (.vercel-token.txt).
- Baseline dump de BD (necesita DATABASE_URL) — ver supabase/migrations/README.md.
- Siguiente Fase 5: CobroSheet, onboarding/Chispa, DayListView (~1.5K) siguen dentro
  del calendario. Regla: nada por encima de ~800 líneas.
- knip.json apareció (análisis de dependencias) — integrarlo en CI sería natural.
