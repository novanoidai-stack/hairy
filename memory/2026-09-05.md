
## Paso 4 de la spec 1: el sentido INVERTIDO, con trinquete

- **Decisión de diseño (Carlos, antes de tocar nada):** el prompt pedía retirar
  `trg_seed_fases_from_cita` y `trg_resync_fases_de_cita` a pelo, pero medido
  contra el árbol TODOS los flujos de mover/editar citas (drag & drop, móvil,
  Chispa, RPCs del portal) escriben las 4 marcas y el cliente PREFIERE
  `cita_fases`: quitar la proyección dejaba las fases en el hueco viejo tras
  cada arrastre. Se hizo **trinquete con guarda de `pg_trigger_depth()`**:
  el resumen nuevo (fases→marcas) propaga a profundidad ≤ 2; el resync solo a
  profundidad 1. El eco del 30 ago es estructuralmente imposible: solo
  realimenta la escritura iniciada por el usuario.
- Migración `20260905110000` aplicada a producción (Management API, registrada
  a mano en schema_migrations). El resumen usa las fórmulas corregidas:
  `fin_activa` = inicio del PRIMER reposo (no el fin de la primera activa).
  `citas_normalizar_fases` pierde su rama de plantilla (la decisión (b) de
  20260904151604 ya avisaba de que moría aquí).
- **Ensayo begin/rollback ANTES de aplicar:** regresión 0, duración cambiada 0,
  0 citas sin fases. Fases 2.940 → 2.968 (559 reposos, +14): el backfill por
  plantilla dio **7 citas con dos reposos** que ya existían de servicios
  técnificados (la nota del 4 sep diciendo "aún no hay citas con plantilla"
  era falsa). 13 citas cambiaron alguna marca: 7 por plantilla (by design) y
  6 formas degeneradas `fa=fe<fin` normalizadas a `fa=fe=fin` — misma
  ocupación exacta.
- **Prueba real en producción (cita Balayage 5 fases / 2 reposos, prof real,
  borrada al final):** criterio 1 ✓ (se crea con dos bandas y el resumen
  correcto), criterio 2 ✓ (estirar el primer reposo +10 no mueve inicio ni
  fin, fin_espera lo sigue). Criterio 3 en su alcance del paso 4 ✓:
  `disponibilidad_publica` ofrece hueco DENTRO del primer reposo (15:15 en
  reposo [15:15,15:40)). El hueco en el SEGUNDO reposo es el paso 5 — el
  propio plan §7 lo coloca ahí ("a partir de aquí: criterios 1, 2 y 3"); el
  prompt de sesión lo contradecía y el plan tenía razón.
- Verificación: tsc 0, vigilar 0 bloqueantes (301 avisos), vigilar:bd 0
  bloqueantes (317) con el tripwire en verde, vigilar:test 395/395, deno
  481/481, smoke publico 24/24 (2 errores de cleanup de workers de Playwright,
  ajenos a los tests).
- Cómo se ejecutó el SQL transaccional contra producción: token del CLI
  (`sbp_...` del gestor de credenciales de Windows, leído con CredRead) contra
  `POST /v1/projects/<ref>/database/query` de la Management API, que admite
  `begin ... rollback` multi-sentencia en una sola llamada. Helper:
  `scripts/q.py`. Ojo: Cloudflare bloquea el User-Agent de Python (403 1010);
  hay que ir por curl.
- Pendiente: paso 5 (que `ventanas_activas_cita` mire `cita_fases`; cruzando
  ese umbral las 8 funciones del grupo A se vuelven multi-fase a la vez) y la
  UI incremental. Criterio 6 ("el salón real la ha usado una vez") sigue
  pendiente: las pruebas las hizo el agente.

## Tarde: paso 5 + UI incremental (la spec 1, completa)

- **Paso 5 APLICADO** (migración `20260905130000`, ensayada antes en
  begin/rollback): `ventanas_activas_cita(p_cita_id, ...)` devuelve las
  ventanas de las fases NO-reposo cuando existen y cae a las 4 marcas si no.
  Las 8 del grupo A parcheadas por ancla (si el ancla falta, la migración
  revienta). Equivalencia medida sobre las 1.977 citas: solo cambian las 7
  de dos reposos, y exactamente liberando el segundo. Criterio 3 ✓ en vivo:
  slot 16:15 ofrecido dentro del reposo2 [16:15,16:30). Sin regresión de
  rendimiento en disponibilidad.
- **Trampas del ensayo**: `generate_series(date,date,int)` no existe (hay que
  castear a timestamp); y `timezone('Europe/Madrid', 'literal')` con el
  literal en unknown devuelve timestamp NAIVE y se reinterpreta — las citas
  de prueba acabaron 2 h desplazadas (inofensivo, los sondas comparaban
  contra lo almacenado). Slots de disponibilidad en rejilla de 15': para
  que un reposo de 15' ofrezca hueco tiene que empezar en múltiplo de 15'.
- **Tripwire retirado** según su propio decreto: RPC `regresion_citas_fases_v2`
  fuera, vigilante `bd-regresion-fases.mjs` + test borrados, edge
  `ejecutar-vigilancia-bd` redeployada. La foto
  `respaldos.citas_antes_de_fases_v2` se queda como histórico cerrado.
- **UI incremental**:
  - Bandas de TRANSICIÓN en la tarjeta del timeline (azul, se distinguen del
    reposo rayado).
  - Borde ARRASTRABLE entre fases en la tarjeta web (FaseBorderHandle):
    escribe los dos updates en `cita_fases`, snap de 5', mínimo 5' por fase,
    bloqueado mientras corre un reloj de reposo. El comparador del memo
    aprendió a comparar `cita_fases` (una frontera puede moverse sin cambiar
    inicio/fin y la tarjeta se quedaría congelada).
  - Botones ±5′ por frontera en FasesCitaPanel (móvil incluido).
  - Cinta «durante sus reposos cabe»: `serviciosCompatiblesReposo.ts`
    REESCRITO sobre `cita_fases` (la C14 original borrada) y enchufado al
    panel con el catálogo vivo del salón.
  - `calcularMarcasResumen()` corregido a la fórmula del servidor
    (fin_activa = inicio del primer reposo).
- Verificación: tsc 0 · vigilar 0 (303) · vigilar:bd 0 (317) ·
  vigilar:test 393/393 · deno 482/482 · smoke publico 24/24.
- Criterio 6 sigue siendo la deuda: el salón real aún no lo ha usado.
