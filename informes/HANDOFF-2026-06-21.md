# HANDOFF — 21 jun 2026 (pulido UI desplegado: landing/login/agenda/reseñas + demo rehecha)

> Continúa el trabajo del HANDOFF-2026-06-20.md (sección C). Todo lo de la sección C de aquel
> handoff (C1–C7) está HECHO y desplegado a `master`. Este doc recoge: (A) cómo probar, (B) lo
> hecho esta sesión, (C) lo que falta validar/afinar, (D) pendientes de antes que siguen, (E) prompt.
>
> Regla del repo: producción despliega de `master`. `npm run build:web` tras tocar `app/`, `lib/`,
> `components/`. Verificar por DOM en el preview (los screenshots se cuelgan por la animación de
> canvas; partículas y spotlight in-app dependen de `requestAnimationFrame`, pausado en el preview).

---

## A. Cómo ejecutar / probar
```bash
npm run build:web          # compila la app web a web/app (tras tocar app/, lib/, components/)
node scripts/serve-web.mjs # espejo local de Vercel (las herramientas de preview lo arrancan)
npx tsc --noEmit           # typecheck (ignorar errores de supabase/functions: son Deno)
```
- Landing `/` · Login `/acceso.html` · Demo sin gastar visitas `/demo.html?share=1`
- Software `/app` · Caja `/app/caja` · Reseñas `/app/resenas` · Config `/app/configuracion`
- Detalle en `informes/MEGA_INFORME_MECHA.md` (tiene **adenda 20 jun** con todo lo de esta sesión).

---

## B. HECHO esta sesión (todo en `master`, desplegado por fases)

1. **Bugs rápidos** (`dda65b7d`): "Caja" en el Sidebar de escritorio (`components/layout/Sidebar.tsx`);
   scroll de Reseñas (`app/(tabs)/resenas.web.tsx`).
2. **Login** (`4126f815`, `web/acceso.html`): partículas + lecho rocoso fuera; dos fondos lisos
   ceniza; carga "Conectando con Google" solo loader sobre ceniza (sin franjas ni divisorias);
   "atrás" del navegador vuelve al login (pushState/popstate).
3. **Landing** (`a22e5751`, `web/index.html`): CTA "¿Quieres el acceso ya?"; fondo ceniza (agua/
   espacio); partículas/lecho desaturados a gris ceniza; FPS optimizados (cap render/lecho, dpr 1.5,
   menos partículas).
4. **Agenda** (`1577762c`, `components/agenda/AgendaCalendar.web.tsx`): rejilla crema de marca (no
   blanco bloc de notas); KPIs y mini-calendario colapsables de forma independiente.
5. **Demo/tutorial** (`661ab136`, `web/demo.html` + `app/(tabs)/configuracion.web.tsx`): tour
   cinemático rehecho (controles fijos abajo-centro, sin contador, título de fase fijo arriba-centro,
   texto cinemático con reveal izq→der en la zona oscura, paso 1/fullscreen con banda scrim);
   tutorial 2 enfoca sub-sección concreta + resalta la pestaña activa.

---

## C. A VALIDAR / AFINAR (en navegador real — no verificable en el preview por rAF)

1. **Tour general — lado del texto cinemático por paso**: está puesto en cada paso vía `side`
   (y fallback a `guidePos`) en los arrays `TUTORIAL_GENERAL`/`TUTORIAL_CONFIG` de `web/demo.html`.
   Si algún paso cae el texto sobre la zona ILUMINADA (en vez de la oscura), cambiar su `side`
   ('left'/'right'/'center'). Hoy: agenda/ficha/IA/equipo = center (banda); cita/cliente/servicio/
   hora/estado/reposos/fórmula/avisos/informes = left; config = center.
2. **Partículas ceniza de la landing**: confirmar que el gris ceniza y el FPS quedan bien en
   pantalla real (no se puede ver en el preview). Tonos en el array `COLORS` y el fondo en
   `.bg`/`.bg::before` de `web/index.html`.
3. **Tutorial 2 — granularidad del spotlight**: hoy enfoca la **primera sección** de cada pestaña
   (p. ej. "Datos del negocio"). Si se quiere enfocar sub-secciones aún más concretas (p. ej.
   "lunes y martes" en Horarios), hay que añadir `ref`s por sub-sección dentro de los componentes
   `Tab*` de `configuracion.web.tsx` y mapear cada `config-*` → su ref (hoy se coge
   `contentRef.firstElementChild`). La pestaña activa ya se resalta (z-index 1001).

---

## D. Pendiente de antes (sigue vigente)
- **Manual (Carlos):** rotar credenciales Google de `Documentacion/n8n/`; activar "Leaked password
  protection" en Supabase Auth; redeploy del edge `send-reset` (tildes del correo — es de Alexandro).
- **Externo:** DNS de `mecha.app` a Vercel + app Meta a producción (clientes reales).
- **UI (Carlos):** badge "señal pagada" en la ficha de la cita.
- **POS-2** (cobro electrónico/Stripe Terminal/QR) = Alexandro · **POS-3** (VeriFactu/fiscal) =
  fiscalista. Matching automático de lista de espera (SQL) + agente de voz Retell = Alexandro.

---

## E. PROMPT para la siguiente sesión (pegar tal cual)

```
Continuamos Mecha (repo Hairy). Lee informes/HANDOFF-2026-06-21.md (sección C: lo que falta
validar/afinar) y informes/MEGA_INFORME_MECHA.md (adenda 20 jun). Producción despliega de master:
build con npm run build:web, prueba con el preview (verifica por DOM; partículas y spotlight in-app
dependen de requestAnimationFrame y no se ven en el preview), y haz commit+push por fases.

Prioridad: validar en navegador real el tour de la demo (lado del texto cinemático por paso),
el aspecto de las partículas ceniza de la landing, y la granularidad del spotlight del tutorial 2.
Ajustar el campo `side` de los pasos en web/demo.html donde el texto caiga sobre la zona iluminada.
Si se quiere, añadir refs por sub-sección en configuracion.web.tsx para enfocar sub-secciones más
concretas en el tutorial 2.

No inventar diseño no definido; ante duda, preguntar. Código en inglés, comentarios en español,
sin emojis en UI. Multi-tenant por negocio_id. Al cerrar, actualiza el MEGA_INFORME y deja handoff.
```
