# 2026-08-14 — Mega-Plan Mecha: FASE 0 completada

## Contexto
El owner (Caloca) pegó el "Mega-Plan Mecha" (dinero real + agenda + antifraude + tickets).
Decisión: ejecutar por fases. Esta sesión = **FASE 0 (cimientos: migraciones + baseline)**.
Project ref Supabase: `vtrggiogjrhqtwbhbgia`. Token PAT entregado en sesión (rotar).

## Acceso DB (útil para futuras sesiones)
- Sin `supabase` CLI ni `DATABASE_URL`. El PAT funciona contra la **Management API**:
  `POST https://api.supabase.com/v1/projects/{ref}/database/query` con body `{"query": "..."}`.
- **Soporta DDL multi-statement** (verificado). Corre como rol privilegiado (bypass RLS).
- Helper reutilizable: `.zcode/tmp/apply_migration.mjs` (envuelve en BEGIN/COMMIT).
  Uso: `SB_TOKEN=... REF=... node .zcode/tmp/apply_migration.mjs <ruta_sql>`

## FASE 0 — hecho y verificado
- baseline `npx tsc --noEmit` = **limpio** (exit 0).
- **Aplicadas** (todas verificadas en DB tras aplicar):
  - `auditoria-registros-inmutables.sql` (NO estaba aplicada — el plan daba por hecho que sí). Crea tabla `auditoria_registros` + RPCs `registrar_evento_auditoria` / `obtener_auditoria_historica`.
  - `auditoria-citas-trigger.sql` (NUEVA): trigger `citas_audit_cambio_estado` AFTER UPDATE cuando cambia `estado`; inserta directo en `auditoria_registros` (captura también service_role). Nunca bloquea la cita.
  - `negocio-datos-fiscales.sql` (NUEVA): añade `razon_social, nif, direccion_fiscal, cp_fiscal, poblacion_fiscal` a `negocio_portal`.
  - `tickets-verifactu.sql` (NUEVA): tabla `tickets_verifactu` (RLS) + `mint_ticket_verifactu(p_cobro_id)` (SECURITY DEFINER, idempotente, advisory lock por negocio) + trigger `cobros_mint_ticket_trigger` AFTER INSERT/UPDATE cuando `estado in ('completado','pagado')`.
  - `fix-subida-imagenes-portal.sql`: faltaba la política SELECT pública en bucket `salon-fotos` (causa real de que no se veían imágenes del portal).
- **Paridad hash verificada**: PL/pgSQL produce IDÉNTICO a `lib/caja/verifactuHash.ts:16` (`17640f55...` para el mismo input). Formato total `toFixed(2)` correcto (1250→"12.50").
- **Advisors limpios**: mis 3 funciones nuevas no son ejecutables por anon/authenticated; 0 tablas sin RLS.

## Diferido (decisión del owner)
- `antifraude-cobros-rls.sql` (WS-7 V1, revoke INSERT/UPDATE de cobros al cliente): **CREADO y verificado seguro por análisis** (grep exhaustivo = 0 escrituras directas; RPCs cobro son owner=postgres SECURITY DEFINER → sobreviven; service_role intacto) pero **NO aplicado**. Se aplica al inicio de FASE 1 (WS-7) con smoke real de caja/cobro (puerta de despliegue del plan).

## CORRECCIONES AL PLAN (importante para FASE 1+)
1. **auditoria-registros NO estaba aplicada** (el plan asumía que sí). Ya arreglado.
2. **Diagnóstico del bug de foto (WS-10) estaba al revés**: `portal_info()` YA incluye `fondo_portal_url` (portal-fondo-url.sql ya aplicado). El bug real era la **política SELECT pública de `salon-fotos` ausente** → ya aplicada. El WS-10 de FASE 4 será más pequeño de lo esperado.
3. **citas.estado en uso real**: `cancelada, completada, confirmada, no_presentada`. No existe `pendiente` aún (el default nuevo de FASE 2 lo introduce). `estadosBloqueanSolape()` debe contemplarlo.
4. **cobros.estado** = `completado` (POS) / `pagado` (online). Vocabulario DISTINTO al de citas. El trigger de tickets gatea ambos.
5. Hay DOS carpetas de migraciones: `supabase/migrations/` (15 canónicas) y `migrations/` (150+ ficheros de trabajo; ahí viven las del plan).

## Pendiente global del Mega-Plan
FASE 1 (dinero real WS-1 + antifraude WS-7 V1/V2) → FASE 2 (agenda WS-2..5) → FASE 3 (auditoría UI WS-6, tickets UI WS-8, registros WS-11) → FASE 4 (productos WS-9, foto WS-10) → FASE 5 (QA Playwright).

## Recordatorios
- 🔐 **Rotar el PAT de Supabase** (el owner dijo que lo haría).
- Regla del repo: tras cambios estructurales React, `npx tsc --noEmit`; tras migraciones, advisors.
