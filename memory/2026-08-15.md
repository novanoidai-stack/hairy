# 2026-08-15 — Bug cobro rápido (42804) + descansos invisibles: ARREGLADO Y VERIFICADO EN PROD

## Error de cobro — CAUSA RAÍZ CONFIRMADA EN VIVO Y CORREGIDA
- Error: `42804 - column 'ref_id' is of type uuid but expression is of type text` (captura del owner).
- Causa: `crear_cobro_walkin` desplegada declaraba `v_ref_id text` y lo insertaba en `cobro_lineas.ref_id` (uuid) SIN cast. Error de tipos → fallaban TODOS los cobros rápidos. Reproducido en vivo antes del fix (probe con set_config role authenticated + jwt claims, en BEGIN/ROLLBACK).
- **Aplicado y verificado en producción** (`migrations/fix-cobros-refid-uuid.sql` vía Management API):
  - `crear_cobro_walkin` con `v_ref_id::uuid` → test real con producto real PASA.
  - `vender_bono` NO EXISTÍA en prod (VentaBonoModal llamaba función inexistente) → creada corregida (v_profesional_id uuid, ref_id sin ::text).
  - CHECK `cobro_lineas.tipo` ahora admite 'bono'.
- `crear_cobro_desde_cita` (cobros normales agenda): **test en vivo PASA** (antes y después del fix). Si el owner vuelve a ver error en cobro de cita, pedir captura exacta — posibles causas by-design: `cita_no_cobrable` (cancelada/no_presentada), `cita_ya_cobrada`, split mixto que no cuadra.
- Edge conocido (no arreglado, hardening futuro): walkin con ref_id uuid inexistente en `productos` → FK violation en `movimientos_inventario` (el update de inventario es best-effort pero el insert de movimiento no). Clientes actuales solo mandan ids reales.
- Método de test reutilizable: `.zcode/tmp/query.mjs` + `test_walkin2.sql`/`test_desde_cita.sql` (transacción + set_config role/jwt + ROLLBACK; cuenta cobros/tickets antes y después para confirmar cero residuo — 68/68 estable).

## Descansos — CHECK corregido en prod + fixes web
- `migrations/fix-bloqueos-tipo-reunion.sql` **aplicado y verificado**: CHECK de `bloqueos_profesional` ahora admite 'reunion' (la UI web+nativa lo ofrecía y la agenda tiene color/etiqueta para él; el INSERT fallaba y el catch silencioso de NewBloqueoModal se lo tragaba).
- Web (ya en repo, tsc limpio): error visible al crear bloqueo (equipo.web.tsx), 'ausencia'→tipos válidos (mi-jornada.web.tsx), WeekView ahora pinta bloqueos intercalados por hora.
- Pendiente futuro: agenda NATIVA no consulta bloqueos/horarios_profesional.

## PAT Supabase
- El primero que pasó el owner dio 401 (typo al copiar). El segundo funcionó. **Owner debe rotarlo** (quedó en el chat). REF=vtrggiogjrhqtwbhbgia.

## ZCode
- Añadido/completado `glm-5.3` en `~/.zcode/v2/config.json` bajo `builtin:zai-coding-plan` (limit output 131072, priority 99). Backup: config.json.bak-20260815. Si no aparece en el picker, reiniciar la app.

## Reconciliación con sesión paralela (WS-9)
- Commit `1fd4e5675` (otra sesión, 20:46) arregló el MISMO bug del walkin por su cuenta (`migrations/fix-crear-cobro-walkin-ref-id.sql`, convierte v_ref_id a uuid) y absorbió mis cambios de WeekView. Además: CobroSheet/caja mandan p_cliente_id en venta suelta + buscador de productos.
- En la BD se aplicaron las dos versiones (la suya primero, la mía después): funcionalmente equivalentes. Desplegada queda la mía (verificada con test real). Sin conflicto.
- Quedó pendiente por si acaso: vigilar que vender_bono/venta de bono funcione en la app (función creada por mí, no probada end-to-end desde UI).
