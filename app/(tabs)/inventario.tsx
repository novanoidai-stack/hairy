import { View } from 'react-native';

// Placeholder nativo requerido por expo-router: la pantalla real de Inventario
// vive en inventario.web.tsx (solo web por ahora).
export default function InventarioScreen() {
  return <View />;
}
