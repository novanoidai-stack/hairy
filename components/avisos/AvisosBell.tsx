// Version nativa: la campana global de avisos es (de momento) solo web.
// En nativo el acceso a los avisos sigue viviendo en la cabecera de la agenda.
export function AvisosBell(_props: { collapsed?: boolean; mode?: 'sidebar' | 'header' }) {
  return null;
}
