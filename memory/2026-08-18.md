# 2026-08-18

## Cobros / bonos / agenda (Hairy)
- Descuento en % añadido a CobroSheet (toggle €/%, botón junto al input). En modo bono aplica solo a productos extra.
- **La BD no tenía la tabla `bonos` ni `consumir_bono_cita`**: el feature de bonos estaba dormido. Creé migration `bonos_tabla_y_consumo_con_productos` (aplicada vía MCP, guardada en supabase/migrations/20260818000000_...). Adaptada al esquema real: RLS por `profiles.negocio_id` (NO `app.negocio_id` como decía el sesion14), `servicios.id` es **uuid**, y `profesionales` usa `profile_id` (no user_id).
- `consumir_bono_cita` ahora acepta `p_lineas_extra`, `p_descuento_cents`, `p_metodo`: antes los productos extra se perdían silenciosamente al cobrar con bono.
- Ticket PDF y modal de Informes muestran "Servicio cubierto por bono 0,00 €" cuando una línea lleva "(Bono)".
- Estadísticas verificadas OK: ProductosVendidosSection (vendedor = cobro.profesional_id) e informes.web (ingresos netos de propina por prof). Ingresos de productos son brutos (sin repartir descuento).
- Agenda: posición de columnas con numerito en la cabecera (sin drag, petición de Carlos), persistida en localStorage `agenda:ordenProf:<negocioId>`. Profesionales de vacaciones (bloqueo tipo 'vacaciones' que cubre el día) no pintan columna: fila superior "De vacaciones" con avatar gris tachado y la agenda se encoge (maxWidth).
- ⚠️ Carlos pegó un token de Supabase en el chat (`sbp_4184...`): pedirle que lo rote.
## Demo: rework cinematico + dudas IA (Hairy)

- **web/demo.html** (y demo_v2.html sincronizado):
  - Motor de pasos rehecho: contador `seq` cancela todo trabajo diferido al navegar; contenido se pinta SINCRONO en cada paso. Adios bug de "paso visible pero en blanco" al pulsar rapido / volver en mitad de animaciones. Eliminado lock `busy` de 240ms.
  - Intro cinematica (1 vez/sesion, `mecha_demo_intro`): anuncia TODOS los modulos (chips), duracion ~3 min, 27 pasos, cuenta demo ficticia, botones Recorrido guiado / Explorar libre.
  - Boton "¿Dudas?" en la barra: panel con WhatsApp (wa.me/34690792975) + formulario IA. Respuesta inline con avatar Chispa y envio por email opcional.
- **supabase/functions/chispa-dudas-demo/** (deploy v4, verify_jwt off): LLM claude-haiku-4.5 via OpenRouter con grounding en KB REAL (`kb.ts`, regenerable desde `kb.md` con `scratch/gen_kb_ts.cjs`) que cubre agenda/reposos, fichas, bonos, presupuestos, fichajes, VeriFactu, caja, comisiones, informes, planes, arquitectura, acceso. Email via SMTP Hostinger (denomailer, secrets SMTP_* ya existentes). Rate limit: misma RPC check_landing_rate_limit.
  - Lecciones: (1) `Deno.readTextFile` estatico crasheaba el worker -> KB como modulo TS importado. (2) Un deploy con regex CORS mal transcrita (una sola barra en \/\/) rompia localhost; verificado con curl OPTIONS.
- Verificado: tsc --noEmit OK; Playwright en localhost:8080 con ?share=1: spam next/prev/salto capitulo siempre renderiza paso correcto, panel dudas responde con IA real, intro solo 1 vez.
- Material de presentacion de Carlos esta en `Escritorio/Material_Novanoid/` (mensajes_whatsapp.txt con 6 mensajes, curriculum_ia_v3.html, foto).
- ⚠️ Carlos pego OTRO token de Supabase en el chat: pedirle rotarlo (2ª vez).

## Chispa vendedora + material agencia (tarde)
- chispa-dudas-demo v6: modo 'landing' (venta: objeciones, pricing, marketplace, cierre a demo/llamada; SMTP bloqueado en ese modo), modelo configurable por secret CHISPA_MODEL (def haiku-4.5), history de conversacion. KB ampliada con secciones 6 (landing/SEO/AIO/marketplace) y 7 (argumentos de venta).
- Widget premium Chispa flotante en web/index.html (#chispaFab + #chispaChat) usando modo landing. Probado en local: saludo + respuesta IA real OK. NOTA: NO incluir credenciales demomarketing en la KB (publica).
- Material agencia: anadido Escritorio/Material_Novanoid/mensaje_5b_formacion_ventas.txt (cursos n8n + agencias IA, cold calling, con notas para rellenar nombres/metricas).
- Todo pusheado a master (los web/* los empaqueto otra sesion en commits propios; la funcion en fa416c3e6 junto a index.html y memory).
