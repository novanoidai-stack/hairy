# 2026-08-11 — Continuación del plan "coherencia de datos + caja unificada + productos + agenda"

## De dónde se retomó
Otra sesión había dejado FASE 1 (scroll agenda) y la mayor parte de FASE 2 ya
commiteadas. Faltaban: 2A (carga de cita_productos en Caja), 2E (productos en
detalle de cita), toda la FASE 3 (coherencia) y el opcional 2F.

## Hecho esta sesión (todo verificado con `tsc --noEmit` = 0 errores)
- **FASE 2A**: Caja carga `cita_productos` de las citas seleccionadas y los pasa
  como `lineasIniciales` al CobroSheet (ticket unificado cita+productos).
- **FASE 2E**: sección "Productos" en `app/screens/agenda-detalle.tsx` (staff).
  - Diferido: badge en `AppointmentCard` (el componente `AgendaView` está
    huérfano, nadie lo renderiza) y portal cliente `cita/[id].web.tsx`
    (discutible mostrar consumo interno al cliente).
- **FASE 3A**: `lib/metricasNegocio.ts` — fuente única (ingresosRealesCents,
  propinasCents, ticketMedioCents, desglosePorMetodo/Canal, ingresosPrevistosCents).
- **FASE 3B/3C** (reconciliado client-side, SIN tocar RPCs de producción):
  - Caja: arqueo = `ingresosRealesCents` (sin propina, no duplica con tarjeta Propinas).
  - Mi Jornada: "Cobrado" = total_cents − propinas_cents (el RPC ya devolvía ambos).
  - Informes: totalCobrado sin propina, catálogo → "Previsto (catálogo)" secundario,
    propina aparte; `facturacionPorProf` usa la lib.
  - Equipo: ingresos reales por profesional vía cobros (fallback catálogo "INGRESOS (prev.)").
  - Agenda: KPI "HOY" usa `esActiva` (no cuenta canceladas/no-show).
- **FASE 3D**: predicado `citaVisible` añadido a `citasMetrics.ts` como hook;
  aplicación uniforme de `oculta_en_calendario` dejada como decisión pendiente
  (documentada en `docs/coherencia-metricas.md`). La parte de columna de fecha
  (cobros→cobrado_at, citas→inicio) ya queda fijada en `metricasNegocio.ts`.
- **FASE 3E**: `lib/metricasNegocio.test.ts` (10 tests, pasan) +
  `docs/coherencia-metricas.md` (tabla métrica→función→fuente). Añadido
  `deno.json` con task `test` (`--sloppy-imports --no-check`, scope a tests puros).

## Diferido / pendiente
- **FASE 2F** (opcional): migración `cita_productos.cobro_linea_id` para trazar
  producto-cobrado ↔ producto-usado-en-cita. Posponible.
- **Aplicar `citaVisible` uniformemente** en Informes/Citas/Mi Jornada cuando se
  decida la política de citas ocultas no-canceladas.
- **Security**: el usuario compartió la service_role key de Supabase; dijo que la
  rotará al terminar. NO se usó (la reconciliación fue client-side). Recordar
  rotar.

## Lección
Los RPCs `mi_jornada_resumen`/`equipo_jornada_ranking` parecían requerir DDL en
producción, pero al inspeccionar el consumo client-side resultó que la propina ya
venía separada / los ingresos se calculaban en el cliente → se pudo reconciliar
sin tocar BD. **Siempre inspeccionar el consumo antes de asumir que hace falta
DDL.**
