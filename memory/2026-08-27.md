# 2026-08-27 (sesión ships)

Cerradas las 5 ships de ships/2026-08-27-*.md (commit 0bb934cac, master, sin push).

- **deno check:edges**: tarea nueva en deno.json con --no-config; el exclude de supabase/functions/ se queda (imports ../../../lib necesitan resolución bundler), documentado en el propio deno.json.
- **merge rama fuera_jornada**: obsoleta — diaCerrado + tests ya estaban en master; la rama quirky-swanson solo tiene un chore que borra web/demo_v2.html (sin mergear, ajeno).
- **respaldo cliente useAvisos**: ahora pasa horarios_profesional + cierres_negocio a analizarAgendaDia (columnas mínimas). Pendiente verificación visual en demo.
- **descarte revive**: opción 1 elegida por usuario — mapa descartesRecientes (tipo→ts, 1h) suprime el respaldo. El servidor seguirá reviviendo el hallazgo en 15 min si el problema persiste (producto pendiente).
- **advisors**: migración endurecer-internas-advisors aplicada en prod: política restrictive using(false) en agenda_ojos_latido y pg_net movida a extensions. Cron vigilar-agenda verificado succeeded tras el cambio. Las 3 RPCs candidatas auditadas: todas intencionales y protegidas (registrar_error_cliente ya tenía rate-limit 40/60s por IP).
- tsc: error preexistente gridRect en AgendaCalendar.web.tsx:9493 (no mío, sigue abierto).
- Usuario pegó una publishable key sbp_... por chat (baja sensibilidad, pero conviene rotarla por higiene).
