# 2026-08-29 (sesión tarde/noche) — Vigilantes fase 2, familia 12: radar de GitHub

## Hecho
- Plan ejecutado: `docs/superpowers/plans/2026-08-29-vigilantes-fase2-radiografia.md` familia 12.
- Login gh CLI como `novanoidai-stack` (keyring).
- Commiteado: `.github/copilot-instructions.md`, `.github/renovate.json`, `.coderabbit.yaml`,
  workflow `.github/workflows/seguridad.yml` (Semgrep + zizmor).
- **CodeQL no estaba disponible al principio** (repo privado de cuenta personal → exige GHAS).
  El usuario hizo el repo público → activado CodeQL default setup vía API, primera análisis verde.
- Semgrep estreno: 3 bloqueantes = falso positivo (3DES-CBC Redsys con node-forge).
  Lección: **`// nosemgrep` va INLINE en la línea del hallazgo** (en comentario 3 líneas arriba no vale).
- zizmor: limpiado a cero — acciones fijadas por SHA con `# vX`, `persist-credentials: false`,
  `permissions: contents: read` en todos los workflows.
- Bugs heredados desenterrados al poner la CI verde:
  1. `verificar:modelos` rojo: OpenRouter dobló precio de gemini-3.7-flash (0.375/1.875 → 0.75/3.75).
     Actualizado catálogo + test de coste (2.25 → 4.5).
  2. `vigilar:test` NUNCA había corrido en CI: `node --test` con globs no funciona en Node 20
     (local pasaba con Node 24). Nació en 47dbb0ddf justo tras la última CI verde y los fallos
     de precio lo tapaban. Fix: Node 22 en CI y Canario.
- CI verde completa por primera vez desde 05507dff. Un smoke flaky en CI (1/17, Supabase real),
  pasó en rerun — ojo con eso, candidato a la familia de flakiness.
- CLAUDE.md decisión 10 extendida con el radar.

## ⚠️ PENDIENTE URGENTE que avisar al humano
- El repo volvió a ser PÚBLICO, pero el historial de git contiene la service_role heredada
  filtrada el 28 ago (ver AGENTS.md / informes/MIGRACION-CLAVES-SUPABASE-2026-08-28.md).
  Si esa clave sigue viva, está expuesta OTRA VEZ. Preguntar si la sustitución por
  sb_secret_ ya está completa y la heredada desactivada.

## Siguiente
- Familia 2 (botones que fallan en silencio) → 3 (BD) → 11 (atribución, misma sesión que 1-3).

## Familia 1 (29 ago, tarde) — RENDIMIENTO MEDIDO, hecho
- `tests/smoke/mediciones.ts`: el smoke mide ms_carga, long tasks (init script, vale
  para el iframe), fps de scroll rAF, peticiones /rest/v1/. Apunta a rendimiento.jsonl.
- `scripts/vigilantes/rendimiento.mjs`: compara contra línea base POR ORIGEN (ci vs
  canario — producción carga 5x más rápido que el espejo local, bases separadas).
  Bloqueante solo carga >3x base y >15 s. `--aprobar [--origen canario]` mueve la base.
- `scripts/vigilantes/peso-bundle.mjs`: base 8,20 MB / entry 1,05 MB, aviso >5%.
- Bases congeladas: tests/smoke/rendimiento-baseline.json (local) y .canario.json
  (descargada del artefacto de la primera corrida real contra producción).
- Falsos positivos descartados a conciencia: fps del runner headless no es fiable
  (solo avisa <30 fps); N+1 simulado (+35 peticiones) sí avisa; 60 s de carga bloquea.
- **Dato para la familia 3: agenda hace ~65-70 peticiones a Supabase por carga.**
- Bug de argv cazado: `argv[indexOf('--x')+1]` con -1 devuelve argv[0] en silencio.
- Lección CI: no pushear mientras ves una corrida — la concurrency del workflow
  cancela la E2E a medias y parece un fallo.
