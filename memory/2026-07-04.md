# Daily Log: 2026-07-04

## Work Done
1. **Manual Screenshots Loading & Cropping**:
   - Updated `components/manuals/ManualPanel.web.tsx` to handle dynamic `/app` prefix detection (local vs Vercel deployments) via `getCapturaUrl` helper.
   - Prevented blurry stretching of small button screenshots by containing them within a flexbox and limiting `maxWidth` and `maxHeight`.
   - Enabled pulsing red target highlight overlays on screenshots via coordinate mapping (`highlight: { top, left, width, height }`).
   - Cleaned up `/app/manuals/` prefixes in all manual ts definition files in `lib/manuals/` to unify them.

2. **Inventory Category Filters Bug**:
   - Added `minHeight: '40px'` and `alignItems: 'center'` to the `categoryTabs` style definition in `app/(tabs)/inventario.web.tsx` to prevent vertical squishing.
   - Injected Webkit scrollbar hiding css classes (`.no-scrollbar::-webkit-scrollbar { display: none; }`) and added the `className="no-scrollbar"` to the tabs container.

3. **Global Alert Icon & Notifications Bell**:
   - Enabled the notification bell icon button (`AvisosBell`) in the header of all 11 tab screens next to the manual help button.
   - Updated `AvisosBell.web.tsx` to support a clean `mode="header"` layout that positions the dropdown panel relative to the button on desktop and fixed centered on mobile devices.

4. **Notification Navigation & Deep Linking**:
   - Changed the routing action in `AvisosBell` and `AvisosSheet` from `/(tabs)?cita=${id}` to `/(tabs)/?cita=${id}` (with a slash) to ensure Expo Router changes active tab back to Agenda.
   - Updated `DetalleCitaModal` callbacks (`onClose` and `onSaved`) in `AgendaCalendar.web.tsx` to clear the URL parameters and reset the selection ref so that clicking on notifications can repeatedly trigger the edit modal.

5. **Validation**:
   - Deleted the stale build folder and compiled the web app successfully using `npm run build:web`.
