// Stub nativo: el coach intra-pagina (S16) es solo web por ahora (la version
// rica, con anclaje al DOM y coach marks, vive en CoachLauncher.web.tsx). En
// nativo no renderiza.
export function CoachLauncher() {
  return null;
}

export default CoachLauncher;
